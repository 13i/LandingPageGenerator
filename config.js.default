
var config = {};

/**
 * Theme
 */
config.theme = "blank";

/**
 * Meta tags
 */
config.meta = {
    "title": "Blank theme demo",
    "description": "Blank theme",
    "keywords": "theme, blank"
};

/**
 * Contacts
 */
config.contacts = [];

    /**
     * Add contact
     */
    config.contacts.push({
        "name": "John Doe",
        "phone": "+33 (0)1 23 45 67 89",
        "email": "johndoe@example.com"
    });

module.exports = config
